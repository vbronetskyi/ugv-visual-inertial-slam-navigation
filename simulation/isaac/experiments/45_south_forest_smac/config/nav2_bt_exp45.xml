<!--
  Exp 44 BT v12: limited recovery (3 retries, short backup 0.4m).
  v11 minimal BT (1 retry, no backup) caused robot to stall - controller
  needs occasional shake-out to escape stuck states. v6's 8×1m caused
  runaway drift. 3×0.4m is middle ground: enough recovery, bounded drift.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="3" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
        </RateController>
        <FollowPath path="{path}" controller_id="FollowPath"/>
      </PipelineSequence>
      <Sequence name="RecoveryActions">
        <ClearEntireCostmap name="ClearLocalCostmap" service_name="local_costmap/clear_entirely_local_costmap"/>
        <BackUp backup_dist="1.0" backup_speed="0.25" error_code_id="{backup_error_code}"/>
        <Wait wait_duration="1.0"/>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
